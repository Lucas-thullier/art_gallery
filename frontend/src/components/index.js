import Navbar from '@components/Navbar'
import ProductList from '@components/ProductList'
import GoToPaginate from '@components/GoToPaginate'

export default {
  'navbar': Navbar,
  'product-list': ProductList,
  'go-to-paginate': GoToPaginate,
}
